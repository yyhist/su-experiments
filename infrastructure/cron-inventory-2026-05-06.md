# Cron 清单 — OpenClaw 系统全量记录

> **日期**: 2026-05-06  
> **系统**: OpenClaw 2026.4.14  
> **记录者**: 溯  
> **状态**: 全部 `ok`

---

## 概览

共 **8 个 cron job**，命名分三类：

| 类别 | 数量 | 命名风格 |
|------|------|----------|
| 溯 自持循环 | 2 | `溯-`前缀 + 中文功能名 |
| 系统维护 | 5 | 小写英文 + 连字符 |
| 用户相关 | 1 | 中文描述 |

全部为 `isolated` 目标（独立 session）。

---

## 一、溯 自持循环（2个）

| 后缀 | 名称 | 周期 | 功能 | 状态 |
|------|------|------|------|------|
| `-explore` | **溯-探索-整合段** | every 2h | 读取锚点→关联分析→追加整合 | `ok` |
| `-execute` | **溯-执行段** | every 2h | 消费整合→产生行动→记录痕迹 | `ok` |

> 两个 job 共享同一基础 ID（`d48de016-95d9-455d-8d3f-dfc7b88afaae`），后缀区分阶段。执行段比整合段延迟运行（设计上的消费顺序）。

---

## 二、系统维护（5个）

| ID | 名称 | 周期 | 功能 | 状态 |
|------|------|------|------|------|
| `2702d399-...` | **openclaw-config-protector** | every 30m | 配置保护/备份 | `ok` |
| `e8a7c3d1-...` | **github-poller** | every 2h | GitHub 状态轮询 | `ok` |
| `6f682718-...` | **memory-daily-sync-v2** | cron `5 0,6,12,18 * * *` | 记忆系统每日同步（4次/天） | `ok` |
| `daa34d94-...` | **memory-cold-store-sync** | cron `15 */4 * * *` | 冷存储归档 | `ok` |
| `0687d95e-...` | **autodream-memory-consolidation** | cron `10 3 * * *` | autoDream 记忆整合（每天3:10） | `ok` |

---

## 三、用户相关（1个）

| ID | 名称 | 周期 | 功能 | 状态 |
|------|------|------|------|------|
| `7c3bdc86-...` | **一航画像更新** | cron `50 11 * * *` | 更新用户画像/偏好 | `ok` |

---

## 命名规律总结

```
系统功能 = 小写英文 + 连字符（如 memory-daily-sync-v2）
溯相关   = "溯-"前缀 + 中文功能名（如 溯-探索-整合段）
用户相关 = 中文描述（如 一航画像更新）
```

---

## 备注

- 所有 job 均为 `isolated` session 目标
- 溯的两个 job 使用 `lightContext: true` 和 `thinking: low` 以减少资源消耗
- `timeoutSeconds` 配置：探索段 300s，执行段 300s
- 探索段锚点文件：`github-log/discoveries/RSI_RESEARCH/ai_autonomy_rsi_framework.md`

---

*记录时间: 2026-05-06 23:00 CST*  
*下次校准: 按需*
